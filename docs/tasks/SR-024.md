# SR-024 — 实现资源详情与版本信息页面

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W4
- **Epic:** EP04
- **Module:** theme
- **Points:** 8
- **Risk:** medium
- **Dependencies:** SR-017, SR-021

## Objective

完成“实现资源详情与版本信息页面”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `web/app/themes/stock-resource-theme/templates/single-download.php`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [ ] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [ ] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [ ] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [ ] 允许路径与其他 Agent 不冲突；
- [ ] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-024` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-024/` 建立证据目录，先写失败测试或可复现检查。
3. 先绑定稳定 DTO/Presenter，不查询自定义表。
4. 覆盖加载、空、错误、无权和禁用状态。
5. 运行可访问性、响应式和构建检查。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: consumes SR-017 public resource/version DTO shape and an access decision array; no API contract change.
- Database/migration: none.
- Events/Hooks: exposes `sr_theme_single_download_model` filter for service-layer data injection.
- Configuration/Feature Flags: none.
- Cache/invalidation: no runtime cache introduced.
- Backward compatibility: adds `single-download.php` template without changing existing templates.
- Observability/audit: none.

## Acceptance criteria

- [x] 兼容性、限制、版本、风险提示同屏
- [x] 购买/下载 CTA 由 AccessDecision Presenter 提供
- [x] 页面源代码不泄露隐藏内容

## Required commands

```bash
npm ci
npm run lint
npm run test
npm run build
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-024/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [ ] capability、对象所有权、Nonce/CSRF；
- [ ] validate/normalize/sanitize/escape；
- [ ] 幂等、事务、唯一键、并发和重放；
- [ ] 日志脱敏、稳定错误码、request_id、审计；
- [ ] 上传/下载/对象存储/路径安全（适用时）；
- [ ] 金额仅用定点/整数，服务端重算；
- [ ] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert SR-024 commit/PR.
- Feature flag / kill switch: override `sr_theme_single_download_model` with unavailable access decision or remove template.
- Data forward-fix or restore: none.
- Reconciliation command: none.
- User-facing degradation: detail page can render visible metadata with disabled CTA if access decision is unavailable.

## Definition of Done

- [ ] 验收项有自动化或可复核证据；
- [ ] 目标模块和受影响回归通过；
- [ ] API/Schema/ADR/运行手册与实现同步；
- [ ] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [ ] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [ ] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-024, REVIEW.
- Files changed: `web/app/themes/stock-resource-theme/templates/single-download.php`, `docs/evidence/SR-024/**`, status/task documentation.
- Contract changes: single detail template consumes public resource/version/access decision arrays.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-024/commands.log`.
- Security/permission/concurrency checks: template renders only public visible fields and ignores injected storage keys, file hashes, internal notes and hidden content.
- Known limitations: default detail data is placeholder model data until repository-backed services inject the real resource.
- Rollback: revert SR-024 commit/PR.
- Next safe task(s): SR-025 or next plan-approved task.
- Commit/PR: pending.
