# SR-007 — 创建 sr-contracts 共享契约包

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W2
- **Epic:** EP02
- **Module:** contracts
- **Points:** 5
- **Risk:** low
- **Dependencies:** SR-006

## Objective

完成“创建 sr-contracts 共享契约包”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-contracts/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [x] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [x] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [x] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [x] 允许路径与其他 Agent 不冲突；
- [x] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-007` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-007/` 建立证据目录，先写失败测试或可复现检查。
3. 先定义接口、DTO、值对象和错误类型。
4. 为非法值和序列化写单元测试。
5. 验证包在没有 WordPress Bootstrap 时可测试。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: package DTOs mirror existing OpenAPI primitives for IDs, money strings, request IDs, errors, entitlements, download token response and order completion/refund events.
- Database/migration: none.
- Events/Hooks: service interfaces and event DTOs added for EDD order completion/refund projection; no hooks registered.
- Configuration/Feature Flags: none.
- Cache/invalidation: none.
- Backward compatibility: new package only; no root Composer autoload wiring in this task.
- Observability/audit: stable `ErrorResponse` and event DTO request/ID primitives added for later audit-safe implementations.

## Acceptance criteria

- [x] 定义 DTO、值对象、服务接口、领域错误
- [x] 包不依赖 WordPress 全局函数
- [x] 单元测试覆盖值对象非法输入

## Required commands

```bash
composer validate --strict
make lint
make test-unit MODULE=contracts
make test-integration
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-007/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [x] capability、对象所有权、Nonce/CSRF；
- [x] validate/normalize/sanitize/escape；
- [x] 幂等、事务、唯一键、并发和重放；
- [x] 日志脱敏、稳定错误码、request_id、审计；
- [x] 上传/下载/对象存储/路径安全（适用时）；
- [x] 金额仅用定点/整数，服务端重算；
- [x] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert SR-007 package/evidence commit.
- Feature flag / kill switch: not applicable; package is not wired into runtime.
- Data forward-fix or restore: none.
- Reconciliation command: `composer --working-dir=packages/sr-contracts test && make lint`.
- User-facing degradation: none; shared contracts only.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [ ] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-007, REVIEW.
- Files changed: `packages/sr-contracts/**`, `docs/evidence/SR-007/**`, `docs/status/**`, `docs/tasks/SR-007.md`.
- Contract changes: pure PHP value objects, DTOs, enums, exceptions and service interfaces added.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-007/commands.log`.
- Security/permission/concurrency checks: no WordPress/EDD bootstrap or globals; invalid IDs, money strings, UUIDs, UTC timestamps and DTO states are rejected by tests.
- Known limitations: root `make test-unit MODULE=contracts` and `make test-integration` targets are absent; alternatives documented.
- Rollback: revert SR-007 package/evidence commit.
- Next safe task(s): wire package into root tooling or consume it from adapter implementation.
- Commit/PR: branch `feat/SR-007-contracts`; PR not created yet.
