# SR-011 — 实现数据库迁移框架与 WP-CLI

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W2
- **Epic:** EP02
- **Module:** migration
- **Points:** 8
- **Risk:** medium
- **Dependencies:** SR-009

## Objective

完成“实现数据库迁移框架与 WP-CLI”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-core/src/Infrastructure/Migration/**`
- `packages/sr-core/src/Cli/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [x] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [x] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [x] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [x] 允许路径与其他 Agent 不冲突；
- [x] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-011` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-011/` 建立证据目录，先写失败测试或可复现检查。
3. 设计 `sr_schema_migrations` 表和迁移 checksum 规则。
4. 实现 Migration 接口、排序、事务能力探测和失败记录。
5. 实现 `wp sr migrate/status/schema:verify` 与 `--dry-run`。
6. 构造 N-1 schema Fixture 并验证升级/中断重试。
7. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
8. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: none.
- Database/migration: introduced framework definition for `sr_schema_migrations` with dynamic prefix token and checksum tracking.
- Events/Hooks: no WordPress hooks registered in this task because plugin bootstrap files are outside allowed paths.
- Configuration/Feature Flags: none.
- Cache/invalidation: none.
- Backward compatibility: new migration/CLI classes only; no existing database state changed.
- Observability/audit: migration failures are represented with version and message for later logging/audit integration.

## Acceptance criteria

- [x] 迁移有唯一版本号且不可被改写
- [x] wp sr migrate/status 支持 dry-run
- [x] 全新安装和重复执行均通过

## Required commands

```bash
wp sr migrate --dry-run
wp sr migrate
wp sr schema:verify
make test-integration
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-011/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [x] capability、对象所有权、Nonce/CSRF；
- [x] validate/normalize/sanitize/escape；
- [x] 幂等、事务、唯一键、并发和重放；
- [x] 日志脱敏、稳定错误码、request_id、审计；
- [x] 上传/下载/对象存储/路径安全（适用时）；
- [x] 金额仅用定点/整数，服务端重算；
- [x] 缓存不会串用户或延迟撤权。

- [x] 独立评审 Agent/人工复核事务、幂等、并发、权限和回滚；
- [x] 至少一个失败/故障注入场景有证据；
- [x] 不把 CI 绿色等同于业务不变量已验证。

## Rollback / forward-fix

- Code rollback: revert SR-011 migration/CLI/evidence commit.
- Feature flag / kill switch: do not register or invoke migration command; no feature flag needed for framework-only code.
- Data forward-fix or restore: no data mutated in this environment.
- Reconciliation command: `php docs/evidence/SR-011/migration-framework-check.php`.
- User-facing degradation: none; command is not wired to runtime in this task.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [x] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-011, REVIEW.
- Files changed: `packages/sr-core/src/Infrastructure/Migration/**`, `packages/sr-core/src/Cli/**`, `docs/evidence/SR-011/**`, `docs/status/**`, `docs/tasks/SR-011.md`.
- Contract changes: introduced migration registry table definition and checksum immutability behavior.
- Migrations: framework only; no live DB mutation.
- Commands and results: see `docs/evidence/SR-011/commands.log`.
- Security/permission/concurrency checks: duplicate versions rejected, checksum mismatch rejected, failed migrations not recorded as applied, dry-run is read-only.
- Known limitations: `wp` is unavailable locally and command registration requires later bootstrap wiring outside SR-011 paths.
- Rollback: revert SR-011 migration/CLI/evidence commit.
- Next safe task(s): SR-012 建立 Request ID、结构化日志与审计接口；SR-013 注册资源分类法与受控词表。
- Commit/PR: branch `feat/SR-011-migration-cli`; PR not created yet.
