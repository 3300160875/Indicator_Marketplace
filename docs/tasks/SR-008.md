# SR-008 — 创建 sr-platform-bootstrap MU Plugin

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W2
- **Epic:** EP02
- **Module:** bootstrap
- **Points:** 5
- **Risk:** low
- **Dependencies:** SR-007

## Objective

完成“创建 sr-platform-bootstrap MU Plugin”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-platform-bootstrap/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [x] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [x] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [x] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [x] 允许路径与其他 Agent 不冲突；
- [x] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-008` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-008/` 建立证据目录，先写失败测试或可复现检查。
3. 实现显式容器和 Provider 注册。
4. 检查版本、依赖和 Feature Flags。
5. 依赖缺失时在后台安全降级，避免前台白屏。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: none; no REST routes or public API shape changed.
- Database/migration: none.
- Events/Hooks: MU plugin schedules `admin_notices` only when platform dependencies fail in wp-admin; no business hooks added.
- Configuration/Feature Flags: added `FeatureFlags` loader for the `SR_*` flags declared in `docs/contracts/feature-flags.yaml`, using safe contract defaults.
- Cache/invalidation: none.
- Backward compatibility: new package only; no WordPress Core, EDD Core, root Composer dependency or existing plugin behavior changed.
- Observability/audit: dependency failures are represented as explicit messages and rendered as an admin error notice instead of a frontend fatal.

## Acceptance criteria

- [x] 检查 WP/EDD/PHP/插件版本
- [x] 加载 Feature Flags 与服务容器
- [x] 依赖缺失时后台明确阻断而非白屏

## Required commands

```bash
composer validate --strict
make lint
make test-unit MODULE=bootstrap
make test-integration
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-008/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [x] capability、对象所有权、Nonce/CSRF；
- [x] validate/normalize/sanitize/escape；
- [x] 幂等、事务、唯一键、并发和重放；
- [x] 日志脱敏、稳定错误码、request_id、审计；
- [x] 上传/下载/对象存储/路径安全（适用时）；
- [x] 金额仅用定点/整数，服务端重算；
- [x] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert SR-008 package/evidence commit.
- Feature flag / kill switch: set `SR_*` options back to contract defaults; package performs no irreversible runtime work.
- Data forward-fix or restore: none.
- Reconciliation command: `composer --working-dir=packages/sr-platform-bootstrap test && make lint`.
- User-facing degradation: frontend requests continue without bootstrap services when dependencies are missing; wp-admin shows an explicit blocking notice.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [ ] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-008, VERIFIED.
- Files changed: `packages/sr-platform-bootstrap/**`, `docs/evidence/SR-008/**`, `docs/status/**`, `docs/tasks/SR-008.md`.
- Contract changes: implements bootstrap-time reading of existing `SR_*` Feature Flag contract names; no new product/API contract.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-008/commands.log`.
- Security/permission/concurrency checks: no frontend fatal on missing dependencies; wp-admin notice output is escaped by `WordPressRuntime`; no mutable shared cache, payment, entitlement or download state touched.
- Known limitations: root `make test-unit MODULE=bootstrap` and `make test-integration` targets are absent; package-level alternatives are documented.
- Rollback: revert SR-008 package/evidence commit.
- Next safe task(s): SR-009 创建五个插件骨架与激活依赖。
- Commit/PR: commit `01351f62df2384513034f1cc2acf977861a52455`; PR https://github.com/3300160875/Indicator_Marketplace/pull/5.
