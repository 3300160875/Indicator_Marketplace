# SR-013 — 注册资源分类法与受控词表

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W3
- **Epic:** EP03
- **Module:** core
- **Points:** 5
- **Risk:** low
- **Dependencies:** SR-009

## Objective

完成“注册资源分类法与受控词表”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-core/src/Content/Taxonomy/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [x] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [x] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [x] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [x] 允许路径与其他 Agent 不冲突；
- [x] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-013` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-013/` 建立证据目录，先写失败测试或可复现检查。
3. 冻结字段/分类法 Schema 和公开/内部边界。
4. 先写 sanitize/auth/状态机测试。
5. 实现服务、后台 UI 与发布 Gate，并写审计。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: no route added; REST term serializer returns stable `taxonomy`, `slug`, `name`, `count` fields for later endpoint work.
- Database/migration: none; uses WordPress taxonomy tables by design.
- Events/Hooks: no runtime hook registration in this task because startup files are outside allowed paths.
- Configuration/Feature Flags: none.
- Cache/invalidation: none.
- Backward compatibility: new taxonomy support classes only.
- Observability/audit: referenced deletion returns stable `referenced_term_requires_migration` for later audit/logging.

## Acceptance criteria

- [x] 平台/指标类型/策略标签/内容类型可管理
- [x] REST schema 返回稳定 slug
- [x] 删除被引用词表需阻断或迁移

## Required commands

```bash
composer validate --strict
make lint
make test-unit MODULE=core
make test-integration
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-013/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [x] capability、对象所有权、Nonce/CSRF；
- [x] validate/normalize/sanitize/escape；
- [x] 幂等、事务、唯一键、并发和重放；
- [x] 日志脱敏、稳定错误码、request_id、审计；
- [x] 上传/下载/对象存储/路径安全（适用时）；
- [x] 金额仅用定点/整数，服务端重算；
- [x] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert SR-013 taxonomy/evidence commit.
- Feature flag / kill switch: do not wire taxonomy catalog into runtime registration.
- Data forward-fix or restore: none.
- Reconciliation command: `php docs/evidence/SR-013/taxonomy-check.php`.
- User-facing degradation: none until runtime wiring is added.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [x] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-013, REVIEW.
- Files changed: `packages/sr-core/src/Content/Taxonomy/**`, `docs/evidence/SR-013/**`, `docs/status/**`, `docs/tasks/SR-013.md`.
- Contract changes: taxonomy definitions, controlled vocabulary slugs and public term schema.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-013/commands.log`.
- Security/permission/concurrency checks: ASCII slug normalization, stable public schema and referenced deletion guard.
- Known limitations: runtime WordPress taxonomy registration is deferred because SR-013 allowed paths only include taxonomy classes.
- Rollback: revert SR-013 taxonomy/evidence commit.
- Next safe task(s): SR-027 配置角色、能力与最小权限；SR-028 实现 StorageService 接口与 MinIO 适配器；SR-031 建立 EddOrderAdapter 与兼容测试。
- Commit/PR: branch `feat/SR-013-taxonomy-vocabulary`; PR not created yet.
