# SR-015 — 实现资源编辑工作台与发布检查

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W3
- **Epic:** EP03
- **Module:** core
- **Points:** 8
- **Risk:** medium
- **Dependencies:** SR-014

## Objective

完成“实现资源编辑工作台与发布检查”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-core/src/Admin/ResourceEditor/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [x] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [x] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [x] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [x] 允许路径与其他 Agent 不冲突；
- [x] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-015` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-015/` 建立证据目录，先写失败测试或可复现检查。
3. 冻结字段/分类法 Schema 和公开/内部边界。
4. 先写 sanitize/auth/状态机测试。
5. 实现服务、后台 UI 与发布 Gate，并写审计。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: none; support classes only.
- Database/migration: none.
- Events/Hooks: no runtime hook registered in this task because SR-015 production code is limited to `Admin/ResourceEditor/**`.
- Configuration/Feature Flags: none.
- Cache/invalidation: none.
- Backward compatibility: new resource editor support classes only.
- Observability/audit: high-risk field changes produce stable `resource_editor.high_risk_change` audit events.

## Acceptance criteria

- [x] 编辑/技术/版权/商业字段分区
- [x] P0 必填未满足时不能发布
- [x] 每次高风险修改写审计

## Required commands

```bash
composer validate --strict
make lint
make test-unit MODULE=core
make test-integration
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-015/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [x] capability、对象所有权、Nonce/CSRF；
- [x] validate/normalize/sanitize/escape；
- [x] 幂等、事务、唯一键、并发和重放；
- [x] 日志脱敏、稳定错误码、request_id、审计；
- [x] 上传/下载/对象存储/路径安全（适用时）；
- [x] 金额仅用定点/整数，服务端重算；
- [x] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert the SR-015 resource editor/evidence commit.
- Feature flag / kill switch: do not wire the support classes into WordPress admin screens or publish hooks.
- Data forward-fix or restore: none; no data mutation is introduced.
- Reconciliation command: `php docs/evidence/SR-015/resource-editor-check.php`.
- User-facing degradation: none until later runtime/UI wiring consumes the support classes.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [x] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-015, implementation ready for REVIEW.
- Files changed: `packages/sr-core/src/Admin/ResourceEditor/**`, `packages/sr-core/tests/run.php`, `docs/evidence/SR-015/**`, status/task documentation.
- Contract changes: required editor sections and P0 publish gate issue codes.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-015/commands.log`.
- Security/permission/concurrency checks: P0 publish checks block incomplete, missing rights evidence or high-risk resources; high-risk field changes emit audit event metadata.
- Known limitations: real WordPress admin UI/runtime hook wiring is deferred because SR-015 production paths only cover `Admin/ResourceEditor/**`.
- Rollback: revert the SR-015 commit.
- Next safe task(s): SR-016 创建 `sr_resource_versions` 表与仓储；SR-021 实现设计令牌与基础组件；SR-027 配置角色、能力与最小权限。
- Commit/PR: pending.
