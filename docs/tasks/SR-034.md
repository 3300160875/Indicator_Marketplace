# SR-034 — 实现订单项业务快照

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W6
- **Epic:** EP06
- **Module:** orders
- **Points:** 8
- **Risk:** medium
- **Dependencies:** SR-031, SR-033

## Objective

完成“实现订单项业务快照”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-core/src/Commerce/OrderSnapshot/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [x] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [x] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [x] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [x] 允许路径与其他 Agent 不冲突；
- [x] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-034` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-034/` 建立证据目录，先写失败测试或可复现检查。
3. 通过 EddOrderAdapter 读取，不直接 SQL EDD 表。
4. 验证对象所有权和退款状态。
5. 为历史订单/缺失用户映射定义安全行为。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: none.
- Database/migration: none.
- Events/Hooks: no runtime hook registered.
- Configuration/Feature Flags: none.
- Cache/invalidation: none; snapshot creation is deterministic and existing snapshots are reused by order item id.
- Backward compatibility: additive `Commerce\OrderSnapshot` namespace; SR-031 adapter and SR-033 checkout snapshot contracts remain unchanged.
- Observability/audit: idempotency key, captured time, refund status and frozen terms/amount metadata support downstream audit persistence.

## Acceptance criteria

- [x] 商品类型/规则版本/价格/资源/套餐元数据冻结
- [x] 后续改商品不改变历史订单含义
- [x] 重复创建快照幂等

## Required commands

```bash
composer validate --strict
make lint
make test-unit MODULE=orders
make test-integration
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-034/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [ ] capability、对象所有权、Nonce/CSRF；
- [ ] validate/normalize/sanitize/escape；
- [ ] 幂等、事务、唯一键、并发和重放；
- [ ] 日志脱敏、稳定错误码、request_id、审计；
- [ ] 上传/下载/对象存储/路径安全（适用时）；
- [ ] 金额仅用定点/整数，服务端重算；
- [ ] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert SR-034 commit/PR.
- Feature flag / kill switch: not applicable until snapshot persistence is wired.
- Data forward-fix or restore: none; no snapshot rows are persisted by this task.
- Reconciliation command: deferred to persistence/repository tasks.
- User-facing degradation: continue reading EDD order adapter data without exposing missing snapshots.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [ ] 独立评审完成（高风险任务必须）；
- [x] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-034, REVIEW.
- Files changed: `packages/sr-core/src/Commerce/OrderSnapshot/**`, `docs/evidence/SR-034/**`, status/task documentation.
- Contract changes: `OrderSnapshotService` creates immutable item business snapshots from `EddOrderAdapter`; existing snapshots are reused for idempotency; incomplete resource/plan/terms metadata fails closed with `invalid_snapshot`.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-034/commands.log`.
- Security/permission/concurrency checks: owner guard, missing user mapping guard, refund order guard, required business metadata guard, no direct SQL/request/EDD runtime calls in OrderSnapshot source.
- Known limitations: snapshot persistence and reconciliation commands deferred.
- Rollback: revert SR-034 commit/PR.
- Next safe task(s): SR-035, SR-036.
- Commit/PR: https://github.com/3300160875/Indicator_Marketplace/pull/35.
