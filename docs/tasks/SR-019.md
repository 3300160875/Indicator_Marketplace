# SR-019 — 实现 SEO 元信息、结构化数据与站点地图扩展

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W4
- **Epic:** EP03
- **Module:** seo
- **Points:** 5
- **Risk:** low
- **Dependencies:** SR-017

## Objective

完成“实现 SEO 元信息、结构化数据与站点地图扩展”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-core/src/Seo/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [ ] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [ ] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [ ] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [ ] 允许路径与其他 Agent 不冲突；
- [ ] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-019` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-019/` 建立证据目录，先写失败测试或可复现检查。
3. 明确 canonical/noindex/410 规则。
4. 只输出公开、真实且不构成收益承诺的数据。
5. 验证结构化数据和站点地图。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: none; SR-019 adds a pure PHP SEO support layer for theme/runtime consumers.
- Database/migration: none.
- Events/Hooks: none in this task; WordPress head/sitemap hook wiring is deferred to a downstream runtime task.
- Configuration/Feature Flags: none.
- Cache/invalidation: none.
- Backward compatibility: additive classes under `packages/sr-core/src/Seo/**`.
- Observability/audit: none; SEO documents are read-only projections.

## Acceptance criteria

- [x] 资源页 canonical/title/description 可控
- [x] 下架页面 noindex 或 410 策略明确
- [x] 不输出收益保证类结构化内容

## Required commands

```bash
composer validate --strict
make lint
make test-unit MODULE=seo
make test-integration
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-019/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [ ] capability、对象所有权、Nonce/CSRF；
- [ ] validate/normalize/sanitize/escape；
- [ ] 幂等、事务、唯一键、并发和重放；
- [ ] 日志脱敏、稳定错误码、request_id、审计；
- [ ] 上传/下载/对象存储/路径安全（适用时）；
- [ ] 金额仅用定点/整数，服务端重算；
- [ ] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert SR-019 commit/PR.
- Feature flag / kill switch: do not call the SEO support classes from runtime/theme wiring.
- Data forward-fix or restore: none; no data is mutated.
- Reconciliation command: rerun `php docs/evidence/SR-019/seo-check.php`.
- User-facing degradation: resource pages can fall back to platform/default WordPress SEO output until runtime wiring is added.

## Definition of Done

- [ ] 验收项有自动化或可复核证据；
- [ ] 目标模块和受影响回归通过；
- [ ] API/Schema/ADR/运行手册与实现同步；
- [ ] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [ ] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [ ] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-019, VERIFIED.
- Files changed: `packages/sr-core/src/Seo/**`, `docs/evidence/SR-019/**`, status/task documentation.
- Contract changes: new pure PHP SEO support classes: `ResourceSeoDocument`, `ResourceSeoPresenter`, `SitemapEntry`, `SitemapEntryFactory`, `SitemapXmlRenderer`.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-019/commands.log`.
- Security/permission/concurrency checks: public SEO output consumes only SR-017 `ResourceView`; downlisted/410 resources emit no structured data; JSON-LD excludes offers, aggregate ratings and earnings promise markers.
- Known limitations: runtime WordPress hook registration is deferred because SR-019 allowed paths are limited to `packages/sr-core/src/Seo/**`.
- Rollback: revert SR-019 commit/PR.
- Next safe task(s): SR-027 配置角色、能力与最小权限；SR-025 实现资源详情页购买/VIP CTA 与 AccessDecision 联动。
- Commit/PR: `cfc354d`, https://github.com/3300160875/Indicator_Marketplace/pull/24.
