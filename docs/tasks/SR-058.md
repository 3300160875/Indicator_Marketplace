# SR-058 — 配置 Nginx 防直链与动态页面缓存例外

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W10
- **Epic:** EP09
- **Module:** infra
- **Points:** 5
- **Risk:** low
- **Dependencies:** SR-055

## Objective

完成“配置 Nginx 防直链与动态页面缓存例外”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `infra/nginx/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [ ] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [ ] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [ ] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [ ] 允许路径与其他 Agent 不冲突；
- [ ] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-058` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-058/` 建立证据目录，先写失败测试或可复现检查。
3. 先写可重复、无真实密钥的环境变量契约。
4. 实现并固定容器镜像/服务健康检查。
5. 验证冷启动、重启、清库重建与失败退出码。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI:
- Database/migration:
- Events/Hooks:
- Configuration/Feature Flags:
- Cache/invalidation:
- Backward compatibility:
- Observability/audit:

## Acceptance criteria

- [ ] EDD 上传目录拒绝公开访问
- [ ] checkout/account/wp-json/download 不被错误缓存
- [ ] 私有对象匿名请求 403

## Required commands

```bash
docker compose config --quiet
make bootstrap
make test-smoke
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-058/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [ ] capability、对象所有权、Nonce/CSRF；
- [ ] validate/normalize/sanitize/escape；
- [ ] 幂等、事务、唯一键、并发和重放；
- [ ] 日志脱敏、稳定错误码、request_id、审计；
- [ ] 上传/下载/对象存储/路径安全（适用时）；
- [ ] 金额仅用定点/整数，服务端重算；
- [ ] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback:
- Feature flag / kill switch:
- Data forward-fix or restore:
- Reconciliation command:
- User-facing degradation:

## Definition of Done

- [ ] 验收项有自动化或可复核证据；
- [ ] 目标模块和受影响回归通过；
- [ ] API/Schema/ADR/运行手册与实现同步；
- [ ] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [ ] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [ ] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status:
- Files changed:
- Contract changes:
- Migrations:
- Commands and results:
- Security/permission/concurrency checks:
- Known limitations:
- Rollback:
- Next safe task(s):
- Commit/PR:
