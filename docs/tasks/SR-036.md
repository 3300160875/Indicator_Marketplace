# SR-036 — 实现个人码人工支付 Gateway

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W7
- **Epic:** EP07
- **Module:** payment
- **Points:** 8
- **Risk:** medium
- **Dependencies:** SR-033

## Objective

完成“实现个人码人工支付 Gateway”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-payment-gateways/src/Gateway/ManualQr/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [x] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [x] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [x] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [x] 允许路径与其他 Agent 不冲突；
- [x] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-036` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-036/` 建立证据目录，先写失败测试或可复现检查。
3. 先写状态机、唯一指纹、锁版本和幂等测试。
4. 凭证只作线索，批准必须核对真实账单。
5. 在事务/可恢复边界完成 EDD 订单并写审计。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: none.
- Database/migration: none.
- Events/Hooks: no runtime hook registered.
- Configuration/Feature Flags: controlled by manual payment enabled config, mapped to runtime `SR_MANUAL_PAYMENT_ENABLED`.
- Cache/invalidation: none.
- Backward compatibility: additive `Gateway\ManualQr` namespace; checkout guard from SR-033 remains unchanged.
- Observability/audit: fingerprint, idempotency key and lock version support downstream audit persistence.

## Acceptance criteria

- [x] Gateway 受 SR_MANUAL_PAYMENT_ENABLED 控制
- [x] 订单保持 pending
- [x] 页面明确人工核验而非自动到账

## Required commands

```bash
make test-unit MODULE=sr-payment-gateways
make test-integration TEST=Payment
make test-concurrency TEST=PaymentApproval
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-036/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [x] capability、对象所有权、Nonce/CSRF；
- [x] validate/normalize/sanitize/escape；
- [x] 幂等、事务、唯一键、并发和重放；
- [x] 日志脱敏、稳定错误码、request_id、审计；
- [ ] 上传/下载/对象存储/路径安全（适用时）；
- [x] 金额仅用定点/整数，服务端重算；
- [ ] 缓存不会串用户或延迟撤权。

- [x] 独立评审 Agent/人工复核事务、幂等、并发、权限和回滚；
- [x] 至少一个失败/故障注入场景有证据；
- [x] 不把 CI 绿色等同于业务不变量已验证。

## Rollback / forward-fix

- Code rollback: revert SR-036 commit/PR.
- Feature flag / kill switch: keep runtime `SR_MANUAL_PAYMENT_ENABLED=false`.
- Data forward-fix or restore: none; no submission rows are persisted by this task.
- Reconciliation command: deferred to payment submission repository and audit tasks.
- User-facing degradation: checkout remains disabled/pending and instructs users that payment requires manual verification.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [x] 独立评审完成（高风险任务必须）；
- [x] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-036, VERIFIED.
- Files changed: `packages/sr-payment-gateways/src/Gateway/ManualQr/**`, `docs/evidence/SR-036/**`, status/task documentation.
- Contract changes: Manual QR intent remains pending, disabled config blocks gateway, approval requires verified bill fingerprint/amount and supports lock-version/idempotency checks.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-036/commands.log`.
- Security/permission/concurrency checks: invalid transitions, lock mismatch, proof-only approval, amount mismatch and idempotent replay covered.
- Independent QA: Dalton PASS after `2e82146`; idempotent replay lock/bill validation behavior rechecked.
- Known limitations: persistence, reviewer capability, audit records and final EDD completion transaction deferred.
- Rollback: revert SR-036 commit/PR and keep `SR_MANUAL_PAYMENT_ENABLED=false`.
- Next safe task(s): SR-037, SR-038.
- Commit/PR: https://github.com/3300160875/Indicator_Marketplace/pull/37.
