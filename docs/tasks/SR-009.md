# SR-009 — 创建五个插件骨架与激活依赖

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W2
- **Epic:** EP02
- **Module:** plugins
- **Points:** 5
- **Risk:** low
- **Dependencies:** SR-008

## Objective

完成“创建五个插件骨架与激活依赖”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-core/**`
- `packages/sr-entitlements/**`
- `packages/sr-payment-gateways/**`
- `packages/sr-private-downloads/**`
- `packages/sr-admin-ops/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [x] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [x] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [x] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [x] 允许路径与其他 Agent 不冲突；
- [x] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-009` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-009/` 建立证据目录，先写失败测试或可复现检查。
3. 创建统一入口、Composer、命名空间和测试骨架。
4. 注册依赖检查与安全停用行为。
5. 逐一激活/停用并验证无跨插件隐式依赖。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: none.
- Database/migration: none.
- Events/Hooks: no hooks registered beyond package entry boot calls; no business events emitted.
- Configuration/Feature Flags: none.
- Cache/invalidation: none.
- Backward compatibility: new package skeletons only; no root Composer dependency or existing plugin behavior changed.
- Observability/audit: runtime dependency checks return explicit missing dependency identifiers for later admin notices/logging.

## Acceptance criteria

- [x] 每个插件可独立激活/停用
- [x] Requires Plugins 与运行时检查正确
- [x] 各插件命名空间、版本和测试目录统一

## Required commands

```bash
composer validate --strict
make lint
make test-unit MODULE=plugins
make test-integration
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-009/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [x] capability、对象所有权、Nonce/CSRF；
- [x] validate/normalize/sanitize/escape；
- [x] 幂等、事务、唯一键、并发和重放；
- [x] 日志脱敏、稳定错误码、request_id、审计；
- [x] 上传/下载/对象存储/路径安全（适用时）；
- [x] 金额仅用定点/整数，服务端重算；
- [x] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert SR-009 package/evidence commit.
- Feature flag / kill switch: deactivate the affected first-party plugin package; skeletons do not mutate data.
- Data forward-fix or restore: none.
- Reconciliation command: run all five package tests plus `make lint`.
- User-facing degradation: missing dependencies keep the skeleton plugin blocked without throwing.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [ ] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-009, REVIEW.
- Files changed: `packages/sr-core/**`, `packages/sr-entitlements/**`, `packages/sr-payment-gateways/**`, `packages/sr-private-downloads/**`, `packages/sr-admin-ops/**`, `docs/evidence/SR-009/**`, `docs/status/**`, `docs/tasks/SR-009.md`.
- Contract changes: ordinary WordPress plugin skeletons with `Requires Plugins: easy-digital-downloads` and runtime bootstrap/EDD dependency guards.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-009/commands.log`.
- Security/permission/concurrency checks: no business rules or mutable state; dependency guard returns missing dependency identifiers without throwing.
- Known limitations: root `make test-unit MODULE=plugins` and `make test-integration` targets are absent; alternatives documented.
- Rollback: revert SR-009 package/evidence commit.
- Next safe task(s): SR-010 创建 `stock-resource-theme` 骨架；SR-012 建立 Request ID、结构化日志与审计接口。
- Commit/PR: branch `feat/SR-009-plugin-skeletons`; PR not created yet.
