# SR-006 — 完成技术 Spike 与 ADR-001～006

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W1
- **Epic:** EP00
- **Module:** architecture
- **Points:** 5
- **Risk:** low
- **Dependencies:** SR-002, SR-003

## Objective

完成“完成技术 Spike 与 ADR-001～006”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `docs/adr/**`
- `docs/spikes/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [ ] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [ ] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [ ] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [ ] 允许路径与其他 Agent 不冲突；
- [ ] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-006` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-006/` 建立证据目录，先写失败测试或可复现检查。
3. 创建隔离 Spike 插件/脚本，验证 EDD 后台人工完成订单时 Hook 是否同步执行及重复调用行为。
4. 构造整单退款与订单项退款，记录可用公开 API、事件时点和 ID。
5. 使用两个数据库连接验证 SELECT FOR UPDATE、唯一键和死锁重试。
6. 验证 MinIO 私有对象、120 秒签名 URL、Range 请求和过期行为。
7. 把结论写入 ADR-001～006；未验证假设不得进入业务实现。
8. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
9. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: none.
- Database/migration: none; disposable probe tables only.
- Events/Hooks: EDD completion/refund hook behavior verified with disposable runtime spike.
- Configuration/Feature Flags: none.
- Cache/invalidation: none.
- Backward compatibility: no runtime code changed.
- Observability/audit: spike findings and evidence stored under `docs/spikes/SR-006-results.md` and `docs/evidence/SR-006/`.

## Acceptance criteria

- [x] 验证 EDD 完成订单 Hook
- [x] 验证 EDD 订单项 ID 和退款数据
- [x] 验证 MariaDB 事务/SELECT FOR UPDATE
- [x] 验证 MinIO 签名 URL 与对象私有性

## Required commands

```bash
make doctor
make lint
make test
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-006/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [x] capability、对象所有权、Nonce/CSRF；
- [x] validate/normalize/sanitize/escape；
- [x] 幂等、事务、唯一键、并发和重放；
- [x] 日志脱敏、稳定错误码、request_id、审计；
- [x] 上传/下载/对象存储/路径安全（适用时）；
- [x] 金额仅用定点/整数，服务端重算；
- [x] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert SR-006 documentation commit.
- Feature flag / kill switch: not applicable.
- Data forward-fix or restore: none; disposable local Docker volumes were removed.
- Reconciliation command: `make doctor && make lint && make test`.
- User-facing degradation: none; documentation/spike only.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [ ] 独立评审完成（高风险任务必须）；
- [ ] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-006, REVIEW.
- Files changed: `docs/adr/**`, `docs/spikes/**`, `docs/evidence/SR-006/**`, `docs/status/**`, `docs/tasks/SR-006.md`.
- Contract changes: ADR-001 through ADR-006 accepted.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-006/commands.log`.
- Security/permission/concurrency checks: EDD duplicate completion/refund semantics, MariaDB locking/unique/deadlock and MinIO private/presigned behavior verified; no secrets committed.
- Known limitations: SR-006 is documentation/spike only; adapter implementation belongs to a later task.
- Rollback: revert SR-006 documentation commit.
- Next safe task(s): start EDD adapter work after independent review verifies SR-006.
- Commit/PR: branch `feat/SR-006-spike-adr`; PR not created yet.
