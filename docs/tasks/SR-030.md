# SR-030 — 实现 COS/OSS/S3 生产适配器契约

- **Status source:** `docs/status/task-status.yaml`
- **Milestone:** W5
- **Epic:** EP05
- **Module:** storage
- **Points:** 8
- **Risk:** medium
- **Dependencies:** SR-028

## Objective

完成“实现 COS/OSS/S3 生产适配器契约”，产生可被后续依赖任务稳定消费的代码、契约、测试和运行证据。 本任务不得顺带实现后续任务，也不得改变产品规则。

## Allowed paths

- `packages/sr-private-downloads/src/Storage/Adapters/**`

## Forbidden by default

- WordPress Core、EDD Core、`vendor/`、构建产物和未声明模块；
- 依赖版本、订单/权益/配额规则、数据库破坏性变更；
- 未经任务授权的新第三方插件、商业 SDK、页面构建器或个人码监听方案。

## Preconditions / Definition of Ready

- [ ] 所有依赖任务均为 VERIFIED 或 DONE，且证据可读取；
- [ ] 相关 ADR、OpenAPI、Schema、字段和 UI 契约已读取；
- [ ] `taskctl.py claim` 成功，当前 Agent 没有第二个 IN_PROGRESS 任务；
- [ ] 允许路径与其他 Agent 不冲突；
- [ ] 未决问题不会改变价格、支付、权益、退款、配额、合规或数据兼容规则。

## Reference implementation sequence

1. 读取 `SR-030` 的依赖任务证据、相关 ADR/OpenAPI/Schema，并确认允许路径未被锁定。
2. 在 `docs/evidence/SR-030/` 建立证据目录，先写失败测试或可复现检查。
3. 以接口和 fake adapter 开始。
4. 验证私有 ACL、HEAD、签名 TTL、删除和错误映射。
5. 集成测试只用临时桶/对象并确保清理。
6. 执行下列命令；任何失败不得通过删测试或降低校验绕过。
7. 更新任务卡的契约/迁移影响、测试结果、回滚和下一安全任务；提交 REVIEW。

> Agent 可以细化步骤，但不得删除安全、测试、迁移和证据步骤；若实际版本行为与文档不同，停止并标记 BLOCKED。

## Contract / data / migration impact — completion required

- API/OpenAPI: no public API/OpenAPI change; adapter-layer PHP contract adds `ObjectStorageProvider`, `S3CompatibleAdapterConfig` and `ProductionStorageAdapterFactory`.
- Database/migration: none.
- Events/Hooks: none.
- Configuration/Feature Flags: runtime config wiring is deferred; this task provides the config object/factory that later wiring can consume.
- Cache/invalidation: none.
- Backward compatibility: existing `MinioStorageAdapter` and `StorageService` contract remain compatible.
- Observability/audit: no new runtime logging or audit event in this adapter-contract task.

## Acceptance criteria

- [x] 以 S3 兼容接口为默认契约
- [x] COS/OSS 差异封装在适配器
- [x] 没有供应商 SDK 泄漏到领域层

## Required commands

```bash
make test-unit MODULE=sr-private-downloads
make test-integration TEST=Storage
make test-security TEST=PrivateObject
git diff --check
python tools/agent/validate_docs.py
```

命令若因本任务之前尚未建立对应 Make target 而不可用，任务必须明确记录替代命令与原因，不能填“未运行”。

## Required evidence

- `docs/evidence/SR-030/commands.log`：命令、版本和退出码；
- 测试报告/覆盖率/trace 或截图；
- 契约、迁移 dry-run、性能或并发报告（适用时）；
- `git diff --stat`、提交 SHA、PR URL；
- 失败场景与回滚验证；
- 不得包含真实凭证、Cookie、Token、密钥或生产个人信息。

## Security and correctness review

- [ ] capability、对象所有权、Nonce/CSRF；
- [ ] validate/normalize/sanitize/escape；
- [ ] 幂等、事务、唯一键、并发和重放；
- [ ] 日志脱敏、稳定错误码、request_id、审计；
- [ ] 上传/下载/对象存储/路径安全（适用时）；
- [ ] 金额仅用定点/整数，服务端重算；
- [ ] 缓存不会串用户或延迟撤权。


## Rollback / forward-fix

- Code rollback: revert the SR-030 commit/PR.
- Feature flag / kill switch: not applicable until runtime config wiring is added.
- Data forward-fix or restore: none; no persistent data is mutated.
- Reconciliation command: none.
- User-facing degradation: existing storage behavior remains unchanged because runtime selection is not wired here.

## Definition of Done

- [x] 验收项有自动化或可复核证据；
- [x] 目标模块和受影响回归通过；
- [x] API/Schema/ADR/运行手册与实现同步；
- [x] 无越界文件、依赖升级或 WordPress/EDD Core 修改；
- [x] 独立评审完成（高风险任务必须）；
- [x] 状态为 REVIEW/VERIFIED，路径锁已按流程释放；
- [x] Completion Report 和下一安全任务已填写。

## Completion report / handoff

- Task / status: SR-030, REVIEW.
- Files changed: `packages/sr-private-downloads/src/Storage/Adapters/**`, `docs/evidence/SR-030/**`, status/task documentation.
- Contract changes: S3-compatible adapter config and production factory support S3, COS, OSS and MinIO endpoint conventions.
- Migrations: none.
- Commands and results: see `docs/evidence/SR-030/commands.log`.
- Security/permission/concurrency checks: private ACL, SigV4 authorization header and vendor SDK leak checks covered by `adapter-contract-check.php`.
- Known limitations: repository-level storage Make targets are not defined yet; direct replacement checks are recorded.
- Rollback: revert SR-030 commit/PR.
- Next safe task(s): SR-026, SR-029.
- Commit/PR: `88fb8cd`, https://github.com/3300160875/Indicator_Marketplace/pull/29.
