# SR-032 command log

Branch: `feat/SR-032-access-price-validation`

```bash
php docs/evidence/SR-032/commerce-access-check.php
```

Exit: 255 before implementation.
Output: expected RED failure because `packages/sr-core/src/Commerce/AccessMode.php` does not exist yet.

```bash
php docs/evidence/SR-032/commerce-access-check.php
```

Exit: 0 after implementation.
Output: `SR-032 commerce access and price checks passed.`

```bash
vendor/bin/pint --test packages/sr-core/src/Commerce docs/evidence/SR-032
```

Exit: 1 before formatting.
Output: Pint reported formatting fixes for new Commerce classes.

```bash
vendor/bin/pint packages/sr-core/src/Commerce docs/evidence/SR-032
```

Exit: 0.
Output: Pint fixed new Commerce files.

```bash
composer validate --strict
```

Exit: 0.
Output: `./composer.json is valid`

```bash
composer --working-dir=packages/sr-core validate --strict
```

Exit: 0.
Output: `./composer.json is valid`

```bash
composer --working-dir=packages/sr-core test
```

Exit: 0.
Output: `sr-core runtime tests: ok`

```bash
vendor/bin/pint --test packages/sr-core/src/Commerce docs/evidence/SR-032
```

Exit: 0.
Output: `{"tool":"pint","result":"passed"}`

```bash
make lint
```

Exit: 0.
Output: `lint: ok`

```bash
make test-unit MODULE=commerce
```

Exit: 2.
Output: `make: *** No rule to make target 'test-unit'. Stop.`
Reason: repository-level target is not defined yet; replacement commands are `composer --working-dir=packages/sr-core test` and `php docs/evidence/SR-032/commerce-access-check.php`.

```bash
make test-integration
```

Exit: 2.
Output: `make: *** No rule to make target 'test-integration'. Stop.`
Reason: repository-level target is not defined yet; replacement command is `php docs/evidence/SR-032/commerce-access-check.php`.

```bash
make test
```

Exit: 0.
Output: `lint: ok`; `test: ok`.

```bash
python3 tools/agent/validate_docs.py
```

Exit: 0.
Output: `OK: 76 tasks, 0 locks, no cycles, task cards/contracts consistent`.

```bash
git diff --check
```

Exit: 0.
Output: no output.

```bash
php docs/evidence/SR-032/commerce-access-check.php
```

Exit: 255 during independent QA fix.
Output: expected RED failure because `ResourcePurchaseValidator` still accepted `ProductType::MembershipPlan` when resource meta also declared `membership_plan`.

```bash
php docs/evidence/SR-032/commerce-access-check.php
```

Exit: 0 after independent QA fix.
Output: `SR-032 commerce access and price checks passed.`
