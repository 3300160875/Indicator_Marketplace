# SR-019 command log

Branch: `feat/SR-019-seo-support`

```bash
php docs/evidence/SR-019/seo-check.php
```

Exit: 255 before implementation.
Output: expected failure because `packages/sr-core/src/Seo/*` classes do not exist yet.

```bash
composer validate --strict
```

Exit: 0
Output: `./composer.json is valid`

```bash
make lint
```

Exit: 0
Output: `lint: ok`

```bash
make test-unit MODULE=seo
```

Exit: 2
Output: `make: *** No rule to make target 'test-unit'. Stop.`
Reason / replacement: module-specific `test-unit` target is not defined; replaced by `php docs/evidence/SR-019/seo-check.php`, direct `php -l` over SR-019 files, targeted Pint test and root `make test`.

```bash
make test-integration
```

Exit: 2
Output: `make: *** No rule to make target 'test-integration'. Stop.`
Reason / replacement: `test-integration` target is not defined; replaced by `php docs/evidence/SR-019/seo-check.php`, which verifies canonical, noindex, 410, JSON-LD and sitemap rendering.

```bash
find packages/sr-core/src/Seo docs/evidence/SR-019 -name '*.php' -print -exec php -l {} \;
```

Exit: 0
Output: no syntax errors in SR-019 implementation or evidence script.

```bash
php docs/evidence/SR-019/seo-check.php
```

Exit: 0
Output: `SR-019 SEO checks passed.`

```bash
vendor/bin/pint --test packages/sr-core/src/Seo docs/evidence/SR-019
```

Exit: 0
Output: `{"tool":"pint","result":"passed"}`

```bash
make test
```

Exit: 0
Output: `lint: ok`, `test: ok`

```bash
git diff --check --cached
```

Exit: 0
Output: no whitespace errors.

```bash
python3 tools/agent/validate_docs.py
```

Exit: 0
Output: `OK: 76 tasks, 0 locks, no cycles, task cards/contracts consistent`

```bash
git diff --stat --cached
```

Exit: 0
Output: 13 files changed, 484 insertions(+), 36 deletions(-).
