# Stock Resource Theme

Server-rendered WordPress theme skeleton for the Stock Resource platform.

SR-010 only establishes the theme shell: theme metadata, `theme.json`, PHP templates and static assets. Resource cards, checkout surfaces, entitlement states and account workflows belong to later tasks.
